
Возвращать в методе типизированный параметр
который может быть одним из видов реализаций какого то интерфейса.

А интерфейс создавался именно с той целью, что в себе объеденить различные возможные варианты
Чтобы мы могли в условиях ограниченной возможности (метод возвращает только один "объект" или данные)
сделать решение более гибким.


Например в регрес тестах изменить метод
	public OrderPKO checkData(Map<String, String> params) {
		// Получаем набор элементов
		Set<Entry<String, String>> set = params.entrySet();
		for (Entry<String, String> element : set) {
			$(By.xpath(element.getKey()))
					.shouldHave(Condition.matchesText(element.getValue()));
		}
		return orderPKO;

	}

	вынести его в repeated код и возвращать нужное значение, в зависимости от того
	где он будет вызываться, чтобы использовать этот метод в любых из цепочек методов.
=====================================================================


